{
  "name": "claude-control",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd packages/server && bun run --watch src/index.ts",
    "test": "pnpm -r test",
    "lint": "bunx biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
